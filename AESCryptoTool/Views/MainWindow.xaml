<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AESCryptoTool.Views"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="AESCryptoTool.Views.MainWindow"
        Title="AESCryptoTool" Height="950" Width="850"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        Icon="/aes_crypto_tool_icon.png">
    <Window.Resources>
        <!-- Color Palette - Deep Ocean Theme -->
        <SolidColorBrush x:Key="PrimaryBg" Color="#0F172A"/>
        <SolidColorBrush x:Key="SecondaryBg" Color="#1E293B"/>
        <SolidColorBrush x:Key="CardBg" Color="#334155"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#38BDF8"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#34D399"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#818CF8"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#FB923C"/>
        <SolidColorBrush x:Key="AccentRed" Color="#F87171"/>
        <SolidColorBrush x:Key="AccentYellow" Color="#FACC15"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#94A3B8"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderColor" Color="#475569"/>
        <SolidColorBrush x:Key="SelectionBg" Color="#475569"/>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <!-- Small Button Style -->
        <Style x:Key="SmallButton" TargetType="{x:Type Button}" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <!-- TabItem Style -->
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="Border" Background="Transparent" 
                                BorderBrush="Transparent" BorderThickness="0,0,0,2" 
                                Padding="20,10">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentPurple}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TabControl}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="InputTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="#252536"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Padding="{TemplateBinding Padding}"
                                          VerticalScrollBarVisibility="Auto"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label Style -->
        <Style TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Padding" Value="0,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Star Button Style -->
        <Style x:Key="StarButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Foreground" Value="{StaticResource AccentYellow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Style for Dark Theme -->
        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainGrid" Margin="15" AllowDrop="True" 
          DragEnter="MainGrid_DragEnter" DragLeave="MainGrid_DragLeave" 
          DragOver="MainGrid_DragOver" Drop="MainGrid_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Keys Config -->
            <RowDefinition Height="*"/>    <!-- Tabs -->
            <RowDefinition Height="Auto"/> <!-- Status Bar -->
        </Grid.RowDefinitions>
        
        <!-- Drop Overlay (hidden by default) -->
        <Border x:Name="DropOverlay" Grid.RowSpan="3" Panel.ZIndex="999"
                Background="#8038BDF8" CornerRadius="12" Visibility="Collapsed"
                BorderBrush="{StaticResource AccentBlue}" BorderThickness="3">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="📥" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="Drop file to load in Batch" FontSize="18" 
                           Foreground="White" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Key/IV Section and Settings -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Expander Grid.Column="0" Header="🔐 Key/IV Configuration (Plaintext)" 
                      Foreground="{StaticResource TextPrimary}" IsExpanded="False" Margin="0,0,10,0">
                <Border Background="{StaticResource CardBg}" CornerRadius="8" Padding="15" Margin="0,10,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                            <Label Content="Key (16/24/32 chars):"/>
                            <Grid>
                                <TextBox x:Name="KeyTextBox" Style="{StaticResource InputTextBox}"
                                     TextChanged="KeyTextBox_TextChanged"/>
                                <Button x:Name="KeyShowButton" Content="👁️" Style="{StaticResource SmallButton}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Width="35" Margin="0,5,8,5" Click="KeyShowButton_Click"
                                    ToolTip="Show/Hide Key"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,10">
                            <Label Content="IV (16 chars):"/>
                            <Grid>
                                <TextBox x:Name="IVTextBox" Style="{StaticResource InputTextBox}"
                                     TextChanged="IVTextBox_TextChanged"/>
                                <Button x:Name="IVShowButton" Content="👁️" Style="{StaticResource SmallButton}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Width="35" Margin="0,5,8,5" Click="IVShowButton_Click"
                                    ToolTip="Show/Hide IV"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="🔄 Reset" Style="{StaticResource SecondaryButton}"
                                Click="ResetKeysButton_Click" Margin="0,0,5,0"
                                ToolTip="Reset to default keys"/>
                            <Button Content="📌 Set Default" Style="{StaticResource SecondaryButton}"
                                Click="SetDefaultKeysButton_Click" Margin="5,0,5,0"
                                ToolTip="Set current keys as new default"/>
                            <Button Content="💾 Save" Style="{StaticResource ModernButton}"
                                Background="{StaticResource AccentGreen}" Click="SaveKeysButton_Click"
                                ToolTip="Save keys to encrypted config" Margin="5,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Expander>
        </Grid>

        <!-- Tab Content -->
        <TabControl x:Name="MainTabControl" Grid.Row="1" Margin="0,10,0,10">
            <!-- Operations Tab -->
            <TabItem Header="Operations">
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Encrypt/Decrypt Panels -->
                        <RowDefinition Height="Auto"/> <!-- Recent Items -->
                    </Grid.RowDefinitions>

                    <!-- Encrypt/Decrypt Panels -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Encrypt Panel -->
                        <Border Grid.Column="0" Background="{StaticResource CardBg}" CornerRadius="12" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="🔒 ENCRYPT" FontWeight="Bold" FontSize="18" 
                                               Foreground="{StaticResource AccentGreen}" VerticalAlignment="Center"/>
                                    <CheckBox x:Name="EncryptAutoDetectCheckBox" Content="Auto-detect" 
                                              IsChecked="True" VerticalAlignment="Center" Margin="20,0,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1">
                                    <Label Content="Input (Plain Text):" FontSize="13"/>
                                    <Grid>
                                        <TextBox x:Name="EncryptInputTextBox" Style="{StaticResource InputTextBox}"
                                                 TextChanged="EncryptInputTextBox_TextChanged"
                                                 KeyDown="EncryptInputTextBox_KeyDown"
                                                 Height="45"/>
                                        <Button Content="📋" Style="{StaticResource SmallButton}"
                                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                                Width="35" Margin="0,5,8,5" Click="EncryptPasteButton_Click"
                                                ToolTip="Paste from clipboard"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
                                    <Button Content="🔐 Encrypt" Style="{StaticResource ModernButton}"
                                            Background="{StaticResource AccentGreen}" Click="EncryptButton_Click"/>
                                    <Button Content="Clear" Style="{StaticResource SecondaryButton}"
                                            Click="EncryptClearButton_Click"/>
                                    <Button x:Name="EncryptFavoriteButton" Content="🏷️" Style="{StaticResource StarButton}"
                                            Click="EncryptFavoriteButton_Click" Margin="10,0,0,0"
                                            ToolTip="Add to bookmarks"/>
                                </StackPanel>

                                <StackPanel Grid.Row="3">
                                    <Label Content="Output (Encrypted):" FontSize="13"/>
                                    <TextBox x:Name="EncryptOutputTextBox" Style="{StaticResource InputTextBox}"
                                             IsReadOnly="True" Height="45"
                                             MouseDoubleClick="EncryptOutputTextBox_MouseDoubleClick"
                                             ToolTip="Double-click to copy"/>
                                </StackPanel>

                                <StackPanel Grid.Row="4" Margin="0,10,0,0">
                                    <Label Content="Note (optional):" FontSize="13"/>
                                    <TextBox x:Name="EncryptNoteTextBox" Style="{StaticResource InputTextBox}" Height="45"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Decrypt Panel -->
                        <Border Grid.Column="2" Background="{StaticResource CardBg}" CornerRadius="12" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="🔓 DECRYPT" FontWeight="Bold" FontSize="18" 
                                               Foreground="{StaticResource AccentPurple}" VerticalAlignment="Center"/>
                                    <CheckBox x:Name="DecryptAutoDetectCheckBox" Content="Auto-detect" 
                                              IsChecked="True" VerticalAlignment="Center" Margin="20,0,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1">
                                    <Label Content="Input (Encrypted Text):" FontSize="13"/>
                                    <Grid>
                                        <TextBox x:Name="DecryptInputTextBox" Style="{StaticResource InputTextBox}"
                                                 TextChanged="DecryptInputTextBox_TextChanged"
                                                 KeyDown="DecryptInputTextBox_KeyDown"
                                                 Height="45"/>
                                        <Button Content="📋" Style="{StaticResource SmallButton}"
                                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                                Width="35" Margin="0,5,8,5" Click="DecryptPasteButton_Click"
                                                ToolTip="Paste from clipboard"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
                                    <Button Content="🔓 Decrypt" Style="{StaticResource ModernButton}"
                                            Background="{StaticResource AccentPurple}" Click="DecryptButton_Click"/>
                                    <Button Content="Clear" Style="{StaticResource SecondaryButton}"
                                            Click="DecryptClearButton_Click"/>
                                    <Button x:Name="DecryptFavoriteButton" Content="🏷️" Style="{StaticResource StarButton}"
                                            Click="DecryptFavoriteButton_Click" Margin="10,0,0,0"
                                            ToolTip="Add to bookmarks"/>
                                </StackPanel>

                                <StackPanel Grid.Row="3">
                                    <Label Content="Output (Decrypted):" FontSize="13"/>
                                    <TextBox x:Name="DecryptOutputTextBox" Style="{StaticResource InputTextBox}"
                                             IsReadOnly="True" Height="45"
                                             MouseDoubleClick="DecryptOutputTextBox_MouseDoubleClick"
                                             ToolTip="Double-click to copy"/>
                                </StackPanel>

                                <StackPanel Grid.Row="4" Margin="0,10,0,0">
                                    <Label Content="Note (optional):" FontSize="13"/>
                                    <TextBox x:Name="DecryptNoteTextBox" Style="{StaticResource InputTextBox}" Height="45"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Recent Items Panel (Moved here) -->
                    <Border Grid.Row="1" Margin="0,20,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="⚡ Recent (click to copy)" FontWeight="SemiBold" FontSize="12"
                                       Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                            <ItemsControl x:Name="RecentItemsControl" Grid.Row="1">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource SmallButton}" 
                                                Click="RecentItemButton_Click" Tag="{Binding}"
                                                MaxWidth="200" ToolTip="{Binding Output}">
                                            <TextBlock Text="{Binding Converter={x:Static local:NoteOrOutputConverter.Instance}}" 
                                                       TextTrimming="CharacterEllipsis" 
                                                       FontFamily="Consolas" FontSize="11"/>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Batch Processing Tab -->
            <TabItem Header="Batch">
                <Border x:Name="BatchDropBorder" Background="{StaticResource CardBg}" CornerRadius="12" Padding="20" Margin="0,15,0,0"
                        AllowDrop="True" Drop="BatchBorder_Drop" DragOver="BatchBorder_DragOver">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header with Row Count -->
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="📂 Batch Processing" FontWeight="Bold" FontSize="16" 
                                       Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="BatchRowCountText" Grid.Column="2" Text="" 
                                       Foreground="{StaticResource TextSecondary}" FontSize="13"
                                       VerticalAlignment="Center"/>
                        </Grid>

                        <!-- File Selection -->
                        <Grid Grid.Row="1" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="BatchFilePathTextBox" Grid.Column="0" 
                                     Style="{StaticResource InputTextBox}" Height="45"
                                     IsReadOnly="True" TextAlignment="Center" VerticalContentAlignment="Center"
                                     Text="📥 Drop file here or click Browse..."
                                     Foreground="{StaticResource TextSecondary}"/>
                            <Button Grid.Column="1" Content="📁 Browse" 
                                    Style="{StaticResource ModernButton}" 
                                    Margin="10,0,0,0" Padding="15,8" Height="45"
                                    Click="BrowseBatchFile_Click"/>
                        </Grid>

                        <!-- Options Row -->
                        <Grid Grid.Row="2" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox x:Name="BatchHasHeaderCheckBox" Grid.Column="0" 
                                      Content="File has header row" IsChecked="True"
                                      Foreground="{StaticResource TextPrimary}" 
                                      VerticalAlignment="Center" Margin="0,0,20,0"
                                      Checked="BatchHasHeader_Changed" Unchecked="BatchHasHeader_Changed"/>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Column:" Foreground="{StaticResource TextSecondary}" 
                                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ComboBox x:Name="BatchColumnComboBox" Width="180" Height="38"
                                          IsEnabled="False" SelectionChanged="BatchColumn_SelectionChanged"/>
                            </StackPanel>

                            <TextBlock x:Name="BatchPatternLabel" Grid.Column="2" Text="Pattern:" 
                                       Foreground="{StaticResource TextSecondary}" 
                                       VerticalAlignment="Center" Margin="20,0,10,0"/>
                            <TextBox x:Name="BatchPatternTextBox" Grid.Column="3" 
                                     Style="{StaticResource InputTextBox}" Height="38" VerticalContentAlignment="Center"
                                     Text=".*PhoneNumber.*" ToolTip="Regex pattern to match column name"/>
                        </Grid>

                        <!-- Operation Selection -->
                        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Operation:" Foreground="{StaticResource TextSecondary}" 
                                       VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <RadioButton x:Name="BatchEncryptRadio" Content="🔐 Encrypt" 
                                         IsChecked="True" Foreground="{StaticResource TextPrimary}"
                                         GroupName="BatchOperation" Margin="0,0,20,0"/>
                            <RadioButton x:Name="BatchDecryptRadio" Content="🔓 Decrypt" 
                                         Foreground="{StaticResource TextPrimary}"
                                         GroupName="BatchOperation"/>
                        </StackPanel>

                        <!-- Output Path -->
                        <Grid Grid.Row="4" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Output:" Foreground="{StaticResource TextSecondary}" 
                                       VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="BatchOutputPathTextBox" Grid.Column="1" 
                                     Style="{StaticResource InputTextBox}" Height="45" VerticalContentAlignment="Center"
                                     Text="(auto-generated)"
                                     Foreground="{StaticResource TextSecondary}"
                                     TextChanged="BatchOutputPath_TextChanged"/>
                            <Button Grid.Column="2" Content="📁" 
                                    Style="{StaticResource SecondaryButton}" 
                                    Margin="10,0,0,0" Padding="10,5" Width="45" Height="45"
                                    Click="BrowseOutputFile_Click" ToolTip="Choose output location"/>
                        </Grid>

                        <!-- Preview Panel / Log Panel -->
                        <Border x:Name="BatchPreviewBorder" Grid.Row="5" Margin="0,0,0,15" 
                                Background="{StaticResource SecondaryBg}" CornerRadius="8" Padding="10"
                                Visibility="Collapsed">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="BatchPreviewHeader" Grid.Row="0" Text="📋 Preview" 
                                           Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,8"/>
                                <DataGrid x:Name="BatchPreviewDataGrid" Grid.Row="1" 
                                          AutoGenerateColumns="False" IsReadOnly="True"
                                          MaxHeight="100" CanUserAddRows="False"
                                          Background="Transparent" BorderThickness="0"
                                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                          HorizontalGridLinesBrush="{StaticResource BorderColor}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="40"/>
                                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <!-- Progress Section -->
                        <Grid Grid.Row="6" Margin="0,0,0,15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ProgressBar x:Name="BatchProgressBar" Grid.Row="0" 
                                         Height="20" Minimum="0" Maximum="100" Value="0"
                                         Background="{StaticResource SecondaryBg}"
                                         Foreground="{StaticResource AccentBlue}"/>
                            <TextBlock x:Name="BatchProgressText" Grid.Row="1" 
                                       Text="Ready to process" 
                                       Foreground="{StaticResource TextSecondary}"
                                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </Grid>

                        <!-- Summary (hidden until processing complete) -->
                        <Border x:Name="BatchSummaryBorder" Grid.Row="7" 
                                Background="{StaticResource SecondaryBg}" 
                                CornerRadius="8" Padding="15" Visibility="Collapsed">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="BatchSummaryText" Grid.Column="0"
                                         Background="Transparent" BorderThickness="0"
                                         Foreground="{StaticResource TextPrimary}"
                                         TextWrapping="Wrap" IsReadOnly="True"
                                         VerticalScrollBarVisibility="Auto"/>
                                <StackPanel Grid.Column="1" Orientation="Vertical">
                                    <Button Content="📋" Style="{StaticResource SecondaryButton}"
                                            Margin="0,0,0,5" Padding="8,5"
                                            Click="CopyBatchSummary_Click" ToolTip="Copy summary"/>
                                    <Button Content="📂" Style="{StaticResource SecondaryButton}"
                                            Padding="8,5"
                                            Click="OpenBatchOutputFolder_Click" ToolTip="Open output folder"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Row="8" Orientation="Horizontal" 
                                    HorizontalAlignment="Right" Margin="0,15,0,0">
                            <Button x:Name="BatchResetButton" Content="🧹 Clear" 
                                    Style="{StaticResource SecondaryButton}" 
                                    Padding="15,10" Margin="0,0,10,0" Width="130" Height="45"
                                    Click="BatchReset_Click"/>
                            <Button x:Name="BatchCancelButton" Content="❌ Cancel" 
                                    Style="{StaticResource SecondaryButton}" 
                                    Padding="15,10" Margin="0,0,10,0" Width="130" Height="45"
                                    Foreground="{StaticResource AccentRed}"
                                    BorderBrush="{StaticResource AccentRed}" BorderThickness="1"
                                    IsEnabled="False" Click="BatchCancel_Click"/>
                            <Button x:Name="BatchProcessButton" Content="🔄 Process" 
                                    Style="{StaticResource ModernButton}" 
                                    Padding="15,10" Width="130" Height="45" IsEnabled="False"
                                    Click="BatchProcess_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>

            <!-- History Tab -->
            <TabItem Header="History">
                <Border Background="{StaticResource CardBg}" CornerRadius="12" Padding="15" Margin="0,15,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="HistoryLabel" Grid.Column="0" Text="📜 History" FontWeight="Bold" FontSize="14" 
                                       Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"
                                       Cursor="Hand" MouseLeftButtonDown="HistoryLabel_Click"
                                       ToolTip="Click to show all history"/>

                            <Grid Grid.Column="1" Margin="20,0">
                                <TextBox x:Name="HistorySearchTextBox" Style="{StaticResource InputTextBox}" Height="35"
                                         TextChanged="HistorySearchTextBox_TextChanged" Padding="30,8,8,8"
                                         ToolTip="Search history by input, output, or note"/>
                                <TextBlock Text="🔍" IsHitTestVisible="False" 
                                           Foreground="{StaticResource TextSecondary}" 
                                           Margin="10,0,0,0" FontSize="13"
                                           VerticalAlignment="Center"/>
                            </Grid>

                            <Button Grid.Column="2" Content="🧹 Clear History" Style="{StaticResource SecondaryButton}"
                                    Click="ClearHistoryButton_Click" ToolTip="Delete all history entries (bookmarks preserved)"
                                    Foreground="{StaticResource AccentRed}"/>
                        </Grid>

                        <DataGrid x:Name="HistoryDataGrid" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="False"
                                  SelectionMode="Single" 
                                  MouseDoubleClick="HistoryDataGrid_MouseDoubleClick"
                                  CellEditEnding="HistoryDataGrid_CellEditEnding"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="#252536"
                                  AlternatingRowBackground="{StaticResource CardBg}"
                                  Foreground="{StaticResource TextPrimary}"
                                  GridLinesVisibility="None"
                                  HeadersVisibility="Column"
                                  CanUserResizeRows="False"
                                  CanUserAddRows="False"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="{x:Type DataGridColumnHeader}">
                                    <Setter Property="Background" Value="#1E1E2E"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                                                <Grid>
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            Padding="8,10">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" 
                                                           Width="5" Cursor="SizeWE" Background="Transparent">
                                                        <Thumb.Template>
                                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                                <Border Background="Transparent" Padding="{TemplateBinding Padding}"/>
                                                            </ControlTemplate>
                                                        </Thumb.Template>
                                                    </Thumb>
                                                    <Rectangle Width="1" HorizontalAlignment="Right" Fill="#7F849C" Opacity="0.3" 
                                                               SnapsToDevicePixels="True" IsHitTestVisible="False"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="{x:Type DataGridRow}">
                                    <Setter Property="Height" Value="40"/>
                                    <Style.Triggers>
                                        <!-- Triggers removed to rely on Cell selection for visibility -->
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="{x:Type DataGridCell}">
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type DataGridCell}">
                                                <Border Background="{TemplateBinding Background}" 
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SelectionBg}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=\{0:MM/dd HH:mm\}}" 
                                            Width="90" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Op" Binding="{Binding Operation}" Width="70" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Input" Binding="{Binding Input}" Width="*" MinWidth="100" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Input}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Output" Binding="{Binding Output}" Width="*" MinWidth="100" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Output}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="100" MinWidth="60" IsReadOnly="False">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Note}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="{x:Type TextBox}">
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Background" Value="#252536"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="🔖" Width="50" MinWidth="50" MaxWidth="50" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding IsFavorite, Converter={x:Static local:BoolToBookmarkConverter.Instance}}"
                                                    FontSize="11"
                                                    Click="HistoryFavoriteButton_Click"
                                                    Tag="{Binding}">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource StarButton}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                                <Setter Property="ToolTip" Value="Remove bookmark"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsFavorite}" Value="False">
                                                                <Setter Property="ToolTip" Value="Add to bookmarks"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="📋" Width="50" MinWidth="50" MaxWidth="50" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="📋" Style="{StaticResource StarButton}"
                                                    FontSize="12"
                                                    Click="HistoryCopyButton_Click" Tag="{Binding}"
                                                    ToolTip="Copy output"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="🗑️" Width="50" MinWidth="50" MaxWidth="50" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="🗑️" Style="{StaticResource StarButton}"
                                                    FontSize="12"
                                                    Click="HistoryDeleteButton_Click" Tag="{Binding}"
                                                    ToolTip="Delete this item permanently"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>
            <!-- Bookmarks Tab -->
            <TabItem Header="Bookmarks">
                <Border Background="{StaticResource CardBg}" CornerRadius="12" Padding="15" Margin="0,15,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="🔖 Bookmarks" FontWeight="Bold" FontSize="14" 
                                       Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>

                            <Grid Grid.Column="1" Margin="20,0">
                                <TextBox x:Name="BookmarksSearchTextBox" Style="{StaticResource InputTextBox}" Height="35"
                                         TextChanged="BookmarksSearchTextBox_TextChanged" Padding="30,8,8,8"
                                         ToolTip="Search bookmarks by input, output, or note"/>
                                <TextBlock Text="🔍" IsHitTestVisible="False" 
                                           Foreground="{StaticResource TextSecondary}" 
                                           Margin="10,0,0,0" FontSize="13"
                                           VerticalAlignment="Center"/>
                            </Grid>

                            <Button Grid.Column="2" Content="🧹 Clear Bookmarks" Style="{StaticResource SecondaryButton}"
                                    Click="ClearBookmarksButton_Click" ToolTip="Remove all bookmarks"
                                    Foreground="{StaticResource AccentRed}"/>
                        </Grid>
                        <!-- Bookmarks DataGrid -->
                        <DataGrid x:Name="BookmarksDataGrid" Grid.Row="1" 
                                  AutoGenerateColumns="False" IsReadOnly="False"
                                  SelectionMode="Single" 
                                  MouseDoubleClick="HistoryDataGrid_MouseDoubleClick"
                                  CellEditEnding="HistoryDataGrid_CellEditEnding"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="#252536"
                                  AlternatingRowBackground="{StaticResource CardBg}"
                                  Foreground="{StaticResource TextPrimary}"
                                  GridLinesVisibility="None"
                                  HeadersVisibility="Column"
                                  CanUserResizeRows="False"
                                  CanUserAddRows="False"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                                  
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="{x:Type DataGridColumnHeader}">
                                    <Setter Property="Background" Value="#1E1E2E"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                                                <Grid>
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            Padding="8,10">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" 
                                                           Width="5" Cursor="SizeWE" Background="Transparent">
                                                        <Thumb.Template>
                                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                                <Border Background="Transparent" Padding="{TemplateBinding Padding}"/>
                                                            </ControlTemplate>
                                                        </Thumb.Template>
                                                    </Thumb>
                                                    <Rectangle Width="1" HorizontalAlignment="Right" Fill="#7F849C" Opacity="0.3" 
                                                               SnapsToDevicePixels="True" IsHitTestVisible="False"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <DataGrid.RowStyle>
                                <Style TargetType="{x:Type DataGridRow}">
                                    <Setter Property="Height" Value="40"/>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.CellStyle>
                                <Style TargetType="{x:Type DataGridCell}">
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type DataGridCell}">
                                                <Border Background="{TemplateBinding Background}" 
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SelectionBg}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=\{0:MM/dd HH:mm\}}" 
                                                    Width="90" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Op" Binding="{Binding Operation}" Width="70" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Input" Binding="{Binding Input}" Width="*" MinWidth="100" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Input}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Output" Binding="{Binding Output}" Width="*" MinWidth="100" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Output}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="100" MinWidth="60" IsReadOnly="False">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Note}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="{x:Type TextBox}">
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Background" Value="#252536"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="🔖" Width="50" MinWidth="50" MaxWidth="50" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding IsFavorite, Converter={x:Static local:BoolToBookmarkConverter.Instance}}"
                                                    FontSize="11"
                                                    Click="HistoryFavoriteButton_Click"
                                                    Tag="{Binding}">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource StarButton}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                                <Setter Property="ToolTip" Value="Remove bookmark"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsFavorite}" Value="False">
                                                                <Setter Property="ToolTip" Value="Add to bookmarks"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="📋" Width="50" MinWidth="50" MaxWidth="50" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="📋" Style="{StaticResource StarButton}"
                                                    FontSize="12"
                                                    Click="HistoryCopyButton_Click" Tag="{Binding}"
                                                    ToolTip="Copy output"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="🗑️" Width="50" MinWidth="50" MaxWidth="50" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="🗑️" Style="{StaticResource StarButton}"
                                                    FontSize="12"
                                                    Click="BookmarksDeleteButton_Click" Tag="{Binding}"
                                                    ToolTip="Remove bookmark"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>

        <!-- Status Bar (Moved to Row 2) -->
        <Border Grid.Row="2" Background="{StaticResource SecondaryBg}" CornerRadius="6" 
                Padding="15,10" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusTextBlock" Text="✓ Ready" FontSize="13"
                           Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="⚙️ Settings" Style="{StaticResource SecondaryButton}"
                    Click="SettingsButton_Click" VerticalAlignment="Center" Height="30" Padding="10,0" Margin="10,0,0,0" RenderTransformOrigin="0.141,0.527" BorderBrush="#FF8F8D8D"/>
            </Grid>
        </Border>
    </Grid>
</Window>
