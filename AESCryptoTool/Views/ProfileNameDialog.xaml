<Window x:Class="AESCryptoTool.Views.ProfileNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="New Profile" Height="220" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Style="{StaticResource ThemedDialogWindow}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter Profile Name:" Grid.Row="0" Margin="0,0,0,10" Foreground="{DynamicResource TextPrimary}"/>
        
        <TextBox x:Name="ProfileNameTextBox" Grid.Row="1" Margin="0,0,0,20" 
                 Style="{StaticResource InputTextBox}" MaxLength="30"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="Cancel" Appearance="Secondary" Click="CancelButton_Click" Margin="0,0,10,0"/>
            <ui:Button Content="Create" Appearance="Primary" Click="OkButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
