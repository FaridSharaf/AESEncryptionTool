<Window x:Class="ConsoleApp1.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ConsoleApp1.Views"
        Title="AES Encryption Tool" Height="800" Width="1100"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">
    <Window.Resources>
        <!-- Color Palette - Modern Dark Theme -->
        <SolidColorBrush x:Key="PrimaryBg" Color="#1E1E2E"/>
        <SolidColorBrush x:Key="SecondaryBg" Color="#2A2A3D"/>
        <SolidColorBrush x:Key="CardBg" Color="#313244"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#89B4FA"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#A6E3A1"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#CBA6F7"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#FAB387"/>
        <SolidColorBrush x:Key="AccentRed" Color="#F38BA8"/>
        <SolidColorBrush x:Key="AccentYellow" Color="#F9E2AF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#CDD6F4"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A6ADC8"/>
        <SolidColorBrush x:Key="BorderColor" Color="#45475A"/>
        
        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>
        
        <!-- Small Button Style -->
        <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
        
        <!-- TextBox Style -->
        <Style x:Key="InputTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#252536"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Padding="{TemplateBinding Padding}"
                                          VerticalScrollBarVisibility="Auto"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Padding" Value="0,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <!-- Star Button Style -->
        <Style x:Key="StarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Foreground" Value="{StaticResource AccentYellow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- CheckBox Style for Dark Theme -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" MinHeight="280"/>
            <RowDefinition Height="*" MinHeight="200"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Key/IV Section -->
        <Expander Grid.Row="0" Header="🔐 Key/IV Configuration (Base64 Format)" Margin="0,0,0,10" 
                  Foreground="{StaticResource TextPrimary}" IsExpanded="False">
            <Border Background="{StaticResource CardBg}" CornerRadius="8" Padding="15" Margin="0,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <Label Content="Key (Base64):"/>
                        <Grid>
                            <TextBox x:Name="KeyTextBox" Style="{StaticResource InputTextBox}"
                                     TextChanged="KeyTextBox_TextChanged"/>
                            <Button x:Name="KeyShowButton" Content="👁️" Style="{StaticResource SmallButton}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Width="35" Margin="0,5,8,5" Click="KeyShowButton_Click"
                                    ToolTip="Show/Hide Key"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,10,0">
                        <Label Content="IV (Base64):"/>
                        <Grid>
                            <TextBox x:Name="IVTextBox" Style="{StaticResource InputTextBox}"
                                     TextChanged="IVTextBox_TextChanged"/>
                            <Button x:Name="IVShowButton" Content="👁️" Style="{StaticResource SmallButton}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Width="35" Margin="0,5,8,5" Click="IVShowButton_Click"
                                    ToolTip="Show/Hide IV"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="10,0,0,5">
                        <Button Content="💾 Save" Style="{StaticResource ModernButton}"
                                Background="{StaticResource AccentGreen}" Click="SaveKeysButton_Click"/>
                        <Button Content="🔄 Reset" Style="{StaticResource SecondaryButton}"
                                Click="ResetKeysButton_Click" Margin="5,5,5,0"
                                ToolTip="Reset keys to default values"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Expander>

        <!-- Main Encrypt/Decrypt Section -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Encrypt Panel -->
            <Border Grid.Column="0" Background="{StaticResource CardBg}" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="🔒 ENCRYPT" FontWeight="Bold" FontSize="18" 
                                   Foreground="{StaticResource AccentGreen}" VerticalAlignment="Center"/>
                        <CheckBox x:Name="EncryptAutoDetectCheckBox" Content="Auto-detect" 
                                  IsChecked="True" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1">
                        <Label Content="Input (Plain Text):" FontSize="13"/>
                        <Grid>
                            <TextBox x:Name="EncryptInputTextBox" Style="{StaticResource InputTextBox}"
                                     TextChanged="EncryptInputTextBox_TextChanged"
                                     KeyDown="EncryptInputTextBox_KeyDown"
                                     Height="45"/>
                            <Button Content="📋" Style="{StaticResource SmallButton}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Width="35" Margin="0,5,8,5" Click="EncryptPasteButton_Click"
                                    ToolTip="Paste from clipboard"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
                        <Button Content="🔐 Encrypt" Style="{StaticResource ModernButton}"
                                Background="{StaticResource AccentGreen}" Click="EncryptButton_Click"/>
                        <Button Content="Clear" Style="{StaticResource SecondaryButton}"
                                Click="EncryptClearButton_Click"/>
                        <Button x:Name="EncryptFavoriteButton" Content="☆" Style="{StaticResource StarButton}"
                                Click="EncryptFavoriteButton_Click" Margin="10,0,0,0"
                                ToolTip="Mark as favorite"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3">
                        <Label Content="Output (Encrypted):" FontSize="13"/>
                        <TextBox x:Name="EncryptOutputTextBox" Style="{StaticResource InputTextBox}"
                                 IsReadOnly="True" Height="45"
                                 MouseDoubleClick="EncryptOutputTextBox_MouseDoubleClick"
                                 ToolTip="Double-click to copy"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Margin="0,10,0,0">
                        <Label Content="Note (optional):" FontSize="13"/>
                        <TextBox x:Name="EncryptNoteTextBox" Style="{StaticResource InputTextBox}" Height="35"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Decrypt Panel -->
            <Border Grid.Column="2" Background="{StaticResource CardBg}" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="🔓 DECRYPT" FontWeight="Bold" FontSize="18" 
                                   Foreground="{StaticResource AccentPurple}" VerticalAlignment="Center"/>
                        <CheckBox x:Name="DecryptAutoDetectCheckBox" Content="Auto-detect" 
                                  IsChecked="True" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1">
                        <Label Content="Input (Encrypted Text):" FontSize="13"/>
                        <Grid>
                            <TextBox x:Name="DecryptInputTextBox" Style="{StaticResource InputTextBox}"
                                     TextChanged="DecryptInputTextBox_TextChanged"
                                     KeyDown="DecryptInputTextBox_KeyDown"
                                     Height="45"/>
                            <Button Content="📋" Style="{StaticResource SmallButton}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Width="35" Margin="0,5,8,5" Click="DecryptPasteButton_Click"
                                    ToolTip="Paste from clipboard"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
                        <Button Content="🔓 Decrypt" Style="{StaticResource ModernButton}"
                                Background="{StaticResource AccentPurple}" Click="DecryptButton_Click"/>
                        <Button Content="Clear" Style="{StaticResource SecondaryButton}"
                                Click="DecryptClearButton_Click"/>
                        <Button x:Name="DecryptFavoriteButton" Content="☆" Style="{StaticResource StarButton}"
                                Click="DecryptFavoriteButton_Click" Margin="10,0,0,0"
                                ToolTip="Mark as favorite"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3">
                        <Label Content="Output (Decrypted):" FontSize="13"/>
                        <TextBox x:Name="DecryptOutputTextBox" Style="{StaticResource InputTextBox}"
                                 IsReadOnly="True" Height="45"
                                 MouseDoubleClick="DecryptOutputTextBox_MouseDoubleClick"
                                 ToolTip="Double-click to copy"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Margin="0,10,0,0">
                        <Label Content="Note (optional):" FontSize="13"/>
                        <TextBox x:Name="DecryptNoteTextBox" Style="{StaticResource InputTextBox}" Height="35"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- History Section -->
        <Border Grid.Row="2" Background="{StaticResource CardBg}" CornerRadius="12" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="📜 History" FontWeight="Bold" FontSize="14" 
                               Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    
                    <Grid Grid.Column="1" Margin="20,0">
                        <TextBox x:Name="HistorySearchTextBox" Style="{StaticResource InputTextBox}"
                                 Height="35" TextChanged="HistorySearchTextBox_TextChanged"
                                 ToolTip="Search history by input, output, or note"/>
                        <TextBlock Text="🔍 Search..." IsHitTestVisible="False" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   Margin="15,0,0,0" FontSize="13"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=HistorySearchTextBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button x:Name="ShowFavoritesButton" Content="⭐ Favorites" 
                                Style="{StaticResource SecondaryButton}" Click="ShowFavoritesButton_Click"/>
                        <Button Content="📋 All" Style="{StaticResource SecondaryButton}" 
                                Click="ShowAllHistoryButton_Click"/>
                    </StackPanel>
                </Grid>
                
                <DataGrid x:Name="HistoryDataGrid" Grid.Row="1" 
                          AutoGenerateColumns="False" IsReadOnly="True"
                          SelectionMode="Single" 
                          MouseDoubleClick="HistoryDataGrid_MouseDoubleClick"
                          Background="Transparent" BorderThickness="0"
                          RowBackground="#252536"
                          AlternatingRowBackground="{StaticResource CardBg}"
                          Foreground="{StaticResource TextPrimary}"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          CanUserResizeRows="False"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#1E1E2E"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Height" Value="40"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
                                    <Setter Property="Foreground" Value="#1E1E2E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Time" 
                            Binding="{Binding Timestamp, StringFormat='{}{0:MM/dd HH:mm}'}" Width="100"/>
                        <DataGridTextColumn Header="Op" Binding="{Binding Operation}" Width="70"/>
                        <DataGridTextColumn Header="Input" Binding="{Binding Input}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="ToolTip" Value="{Binding Input}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Output" Binding="{Binding Output}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="ToolTip" Value="{Binding Output}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="ToolTip" Value="{Binding Note}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="⭐" Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding IsFavorite, Converter={x:Static local:BoolToStarConverter.Instance}}"
                                            Style="{StaticResource StarButton}"
                                            FontSize="14"
                                            Click="HistoryFavoriteButton_Click"
                                            Tag="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="📋" Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="📋" Style="{StaticResource StarButton}"
                                            FontSize="12"
                                            Click="HistoryCopyButton_Click" Tag="{Binding}"
                                            ToolTip="Copy output"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Recent Items Panel -->
        <Border Grid.Row="3" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="⚡ Recent (click to copy)" FontWeight="SemiBold" FontSize="12"
                           Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                <ItemsControl x:Name="RecentItemsControl" Grid.Row="1">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource SmallButton}" 
                                    Click="RecentItemButton_Click" Tag="{Binding}"
                                    MaxWidth="180" ToolTip="{Binding Output}">
                                <TextBlock Text="{Binding Output}" TextTrimming="CharacterEllipsis" 
                                           FontFamily="Consolas" FontSize="11"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="4" Background="{StaticResource SecondaryBg}" CornerRadius="6" 
                Padding="15,10" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusTextBlock" Text="✓ Ready" FontSize="13"
                           Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="⚙️ Settings" Style="{StaticResource SmallButton}"
                        Click="SettingsButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
